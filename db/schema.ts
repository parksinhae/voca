import { sql } from "drizzle-orm";
import { integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
export const words=sqliteTable("words",{id:integer("id").primaryKey({autoIncrement:true}),deckKey:text("deck_key").notNull().default("toeic-rc-day1"),english:text("english").notNull(),korean:text("korean").notNull(),note:text("note").notNull().default(""),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)},(t)=>[uniqueIndex("idx_words_deck_english").on(t.deckKey,t.english)]);
export const progress=sqliteTable("progress",{id:integer("id").primaryKey({autoIncrement:true}),userId:text("user_id").notNull(),wordId:integer("word_id").notNull().references(()=>words.id),level:integer("level").notNull().default(0),correctCount:integer("correct_count").notNull().default(0),wrongCount:integer("wrong_count").notNull().default(0),updatedAt:text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`)},(t)=>[uniqueIndex("idx_progress_user_word").on(t.userId,t.wordId)]);
export const bookmarks=sqliteTable("bookmarks",{id:integer("id").primaryKey({autoIncrement:true}),userId:text("user_id").notNull(),wordId:integer("word_id").notNull().references(()=>words.id),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)},(t)=>[uniqueIndex("idx_bookmarks_user_word").on(t.userId,t.wordId)]);
export const studyDecks=sqliteTable("study_decks",{key:text("key").primaryKey(),folderKey:text("folder_key").notNull(),title:text("title").notNull(),kind:text("kind").notNull().default("RC"),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)});
